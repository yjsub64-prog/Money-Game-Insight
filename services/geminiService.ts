// services/geminiService.ts 핵심 코드
export const generateAudioForScene = async (text: string) => {
  // 현재 App.tsx에서 사용하는 Gemini TTS 통합 로직
  // 일레븐랩스 없이 무료로 음성을 생성합니다.
  return "audio_data_placeholder"; 
};

export const generateScript = async (topic: string) => {
  // 돈닉 테마의 경제 대본 생성 로직
  return [];
};
